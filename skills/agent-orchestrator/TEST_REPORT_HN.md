# Hacker News 任务测试报告

## 📊 测试任务

**原始请求**: "获取HACK news 最热贴子 分析内容 写成博客 发送邮箱"

## 🔍 测试结果对比

### ❌ Task Compiler - 拒绝执行

**原因**: 包含禁止的抽象行为词汇 "分析"

```json
{
  "error": "任务包含禁止的抽象行为词汇",
  "forbidden_words": ["分析"],
  "suggestion": "请使用具体的可执行动作（如：提取/生成/执行/部署）"
}
```

### ✅ Task Decomposer - 允许执行

```json
{
  "tasks": [
    {
      "capability": "research",
      "description": "进行资料调研与分析：获取HACK news 最热贴子 分析内容 写成博客 发送邮箱"
    }
  ]
}
```

## 🔧 修正方案

### 方案 1: 单任务（Task Compiler）

**修正后**: "获取Hacker News最热帖子 提取内容 生成博客 发送邮件"

```json
{
  "tasks": [
    {
      "agent": "research",
      "action": "提取",
      "description": "提取：获取Hacker News最热帖子 提取内容 生成博客 发送邮件",
      "output": "数据文件",
      "verifiable": true
    }
  ]
}
```

### 方案 2: 多任务（更清晰）

#### Task 1: 提取数据
```json
{
  "agent": "research",
  "action": "提取",
  "description": "提取Hacker News最热帖子内容和评论数据并保存为JSON文件",
  "output": "数据文件"
}
```

#### Task 2: 生成博客
```json
{
  "agent": "coding",
  "action": "生成",
  "description": "生成技术博客文章并保存为Markdown文件",
  "output": "代码文件"
}
```

#### Task 3: 发送邮件
```json
{
  "agent": "coding",
  "action": "发送",
  "description": "发送邮件到rzhu@gitlab.cn并返回发送状态码",
  "output": "状态码"
}
```

## 📋 完整执行流程

### 使用 Agent Orchestrator

```bash
AO="python3 /home/ubuntu/clawd/skills/agent-orchestrator/scripts/orchestrator.py"

# 1. 初始化
$AO init hn-blog --goal "获取HN最热帖子生成博客并发送邮件"

# 2. 路由
$AO route hn-blog --request "获取Hacker News最热帖子内容和评论数据，生成技术博客文章，发送邮件到rzhu@gitlab.cn"

# 3. 分解
$AO decompose hn-blog

# 4. 计划
$AO plan hn-blog --mode auto

# 5. 审计（必做）
$AO status hn-blog
$AO pipeline hn-blog
$AO audit hn-blog

# 6. 审批
$AO approve hn-blog --by rzhu

# 7. 执行
$AO run hn-blog --timeout 600
```

### 使用 Task Compiler（快速预览）

```bash
COMPILER="python3 /home/ubuntu/clawd/skills/agent-orchestrator/scripts/task_compiler.py"

# 检查任务描述
$COMPILER --check "获取Hacker News最热帖子 分析内容 写成博客 发送邮箱"
# ❌ 输出: {"valid": false, "forbidden_words": ["分析"]}

# 修正后检查
$COMPILER --check "获取Hacker News最热帖子 提取内容 生成博客 发送邮件"
# ✅ 输出: {"valid": true}

# 生成任务
$COMPILER "提取Hacker News最热帖子内容并保存为JSON文件"
$COMPILER "生成技术博客文章并保存为Markdown文件"
$COMPILER "发送邮件到rzhu@gitlab.cn并返回状态码"
```

## 💡 关键发现

### 1. 抽象行为识别

**禁止词汇**: 分析 / 研究 / 思考 / 优化 / 讨论 / 协作

**修正策略**:
- "分析内容" → "提取内容"
- "研究数据" → "查询数据"
- "思考方案" → "生成方案"
- "优化逻辑" → "重构逻辑"
- "讨论问题" → "提交问题"

### 2. 工具选择建议

| 场景 | 推荐工具 | 原因 |
|-----|---------|------|
| **探索性任务** | Task Decomposer | 允许分析、研究、思考 |
| **自动化执行** | Task Compiler | 严格可验证，无抽象行为 |
| **CI/CD 流程** | Task Compiler | 输出必须可判断成功/失败 |
| **灵活规划** | Task Decomposer | 支持抽象描述 |
| **严格执行** | Task Compiler | 只接受可执行动作 |

### 3. 输出差异

#### Task Decomposer
```
[Task 1] 资料调研与分析
  能力: research
  描述: 进行资料调研与分析：...
```

#### Task Compiler
```json
{
  "agent": "research",
  "action": "提取",
  "description": "提取：...",
  "output": "数据文件",
  "verifiable": true
}
```

## 🎯 最佳实践

### 推荐工作流

```
1. 探索阶段
   ├─ 使用 Decomposer 进行灵活规划
   └─ 允许分析、研究、思考

2. 验证阶段
   ├─ 使用 Compiler 检查任务描述
   └─ compile --check "任务描述"

3. 执行阶段
   ├─ 修正抽象行为词汇
   ├─ 使用 Compiler 生成可执行任务
   └─ 传递给 Orchestrator 执行
```

### 示例对话

**用户**: "分析代码结构，提取API路径"

**Step 1**: 用 Decomposer 规划
```bash
$ decompose "分析代码结构，提取API路径"
# ✅ 成功分解
```

**Step 2**: 用 Compiler 验证
```bash
$ compile --check "分析代码结构，提取API路径"
# ❌ {"forbidden_words": ["分析"]}
```

**Step 3**: 修正后执行
```bash
$ compile "提取代码结构信息，提取API路径"
# ✅ {"tasks": [...], "verifiable": true}
```

## 📊 测试统计

| 指标 | Task Decomposer | Task Compiler |
|-----|----------------|---------------|
| **通过率** | 100% | 0% (含禁止词汇) |
| **修正后通过率** | - | 100% |
| **输出格式** | 文本 + JSON | 仅 JSON |
| **抽象行为** | 允许 | 禁止 |
| **可验证性** | 无要求 | 必须可验证 |

## 📚 相关文档

- [Task Compiler Guide](./TASK_COMPILER_GUIDE.md)
- [Task Decomposer Guide](./TASK_DECOMPOSER_GUIDE.md)
- [Tool Comparison](./TOOL_COMPARISON.md)
- [SKILL.md](./SKILL.md)

## ✅ 结论

**原始任务**: 包含抽象行为 "分析"，被 Task Compiler 拒绝

**修正方案**: 替换为可执行动作 "提取"，通过验证

**推荐流程**:
1. 使用 Decomposer 探索规划
2. 使用 Compiler 验证修正
3. 使用 Orchestrator 执行

---

**测试时间**: 2026-02-15
**测试任务**: Hacker News 最热帖子任务
**测试工具**: Task Decomposer + Task Compiler
